# AI 專屬工作技能 (Skills SOP)

## 技能名稱：匯入 Google 文件並轉換為 Markdown

### 觸發條件
當使用者提供一串 Google Docs 的公開分享連結時。

### 執行流程
1. **設定工作目錄**
   - 確保工作目錄為專案下的 `course` 資料夾。

2. **執行轉換工具**
   - 執行命令：`python import_gdoc.py <Google文件連結>`。
   - 工具運作原理：`import_gdoc.py` 會向 Google 請求該文件的 HTML 匯出版本，並自動過濾掉冗長的 Google 追蹤重新導向網址（`google.com/url?q=`），還原成乾淨的原始超連結。

3. **依賴套件處理 (自動)**
   - 轉換過程依賴 `beautifulsoup4`（負責解析複雜的 HTML 樹狀結構）與 `markdownify`（負責將網頁標籤轉換為標準 Markdown 語法）。
   - 若環境中缺失這兩個套件，需確保透過 `pip` 補齊安裝。

4. **引用與註腳上標格式化規範**
   - 轉換工具 `import_gdoc.py` 內建引用上標格式化邏輯。匯入文章時必須確保所有內文中的引用數字（包括與前文有空格的數字，如 `這份信經 1。`）被自動偵測並格式化為 HTML 上標形式如 `<sup>[1]</sup>`，並自動清除數字前多餘的空白字元。
   - 格式化過程需避免誤判日常計量數字（如 `1年`、`2次`、`10px` 等），只針對跟隨於中英文字符、書名號或括號後並鄰近標點符號的引用編號進行轉換。

5. **格式清洗與防禦規範**
   - **底線轉義還原**：Google Docs 匯出時，`markdownify` 會自動將網址中的底線 `_` 轉義為 `\_`，這會破壞連結匹配。在執行導入時，必須確保將 `\_` 還原為 `_`。
   - **單行表格分割**：若發現手寫的 Markdown 表格被合併在單一行（特徵為單行中包含許多 `|` 分隔符號），必須將其手動或自動分割回多行結構。
   - **公式與控制碼清理**：掃描並清理 Google Docs 匯出時殘留的 `span_` 等格式控制碼。若有未渲染的 LaTeX 數學公式（如 `\text{...}`），必須主動將其改寫為標準 Markdown 或好讀的文字樣式（如 Blockquote 與列表），除非網頁加載了 MathJax/KaTeX 渲染引擎。

6. **驗證與存檔**
   - 轉換成功後，程式會自動以文件標題命名，並儲存為 `.md` 檔案。
   - 確保該檔案正確儲存在 `course` 資料夾內。

7. **後續回報**
   - 向使用者回報產生的完整檔名與路徑。
   - 主動詢問使用者是否需要執行「發布新文章上線」的技能。
